// TODO: when this page ships, remove the robots noindex below, fill in real
// OG/description copy (mirror build/layout.jsx), and add /sites to
// src/app/sitemap.js. Also remove the NODE_ENV gate in page.jsx.
export const metadata = {
  title: 'Sites | Keshav Kunver',
  description: 'TODO: one-line description of the site rebuild/upgrade service.',
  robots: { index: false, follow: false },
};

export default function SitesLayout({ children }) {
  return children;
}
